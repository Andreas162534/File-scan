/*import static org.junit.jupiter.api.Assertions.*;
import org.junit.Test;
//what do I import!
import static org.junit.Assert.assertEquals;
import static org.junit.Assert.fail;

class MyFileTest2 {
    @Test           //why doesnt recognize as Test?  Folder structure?
    public void writePrimes(){
    File inputFile = new File("18, kabel, 13,3");   //is this where I create the file content??
    String actual = inputFile.writePrimes();
    String expected = "13";
    assertEquals(expected, actual);
    }

}*/